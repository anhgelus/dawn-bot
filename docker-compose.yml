version: '3'

services:
  bot:
    build: .
    env_file:
      - .env
    depends_on:
      - db
      - redis
    volumes:
      - ./tmp:/app/config
  db:
    image: postgres:alpine
    env_file:
      - .env
    ports:
      - "5432:5432"
  redis:
    image: redis:alpine